version: 0.1

options:
  - first-option

procedures:
  - proc-type: read
    config:
      file: resources/graphs/graphG6.g6
      graph-format: g6
      number-of-graphs: 5

  - proc-type: colour
    config:
      colouriser-type: sat

  - proc-type: write
    config:
      file: hello
      graph-format: hello

  - proc-type: last-procedure