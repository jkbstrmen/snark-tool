use crate::graph::graph::Graph;
use crate::graph::edge::Edge;
use crate::graph::undirected::edge::UndirectedEdge;

pub fn removable_edges<G: Graph>(graph: &G) -> Vec<UndirectedEdge> {

    // TODO - implement

    unimplemented!()
}